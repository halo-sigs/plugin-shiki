{
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "rspack build",
    "build": "rspack build",
    "prettier": "prettier --write src/"
  },
  "devDependencies": {
    "@types/node": "20"
  }
}
